import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Sample Page',
  description: 'Sample Page page - Access and manage your content',
};

export default function Navigate to="Page() {
  return <Navigate to=" />;
}